package com.quantechs.Licences.Utils;


/*import java.io.StringReader;

import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.internal.Streams;
import com.google.gson.stream.JsonReader;*/

public class Serializers {

    /*public static String serializeNotification(String value) throws JsonParseException
    {
        JsonObject  jsonObject = Streams.parse(new JsonReader(new StringReader(value))).getAsJsonObject();
        String result = jsonObject.get()
        .typeNotification(jsonObject.get("typeNotification").getAsString())
        .titre(jsonObject.get("titre").getAsString())
        .description(jsonObject.get("description").getAsString())
        .message(jsonObject.get("message").getAsString())
        .build();
        return payload;
    }*/
}
